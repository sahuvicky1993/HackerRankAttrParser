#include <iostream>
#include <algorithm>
#include <sstream>
#include <map>
#include <cmath>
#include <cstdio>
#include <vector>
#include <cstring>
using namespace std;
// Following are helper_fn so that we can write main() neatly 
void create_DB(vector<string> input,int op);
void store(vector<string>s );
void show_map(void);
bool shouldContinue(string ip);
void printVector(vector<string>);
void ansToQueries(vector<string> query);

